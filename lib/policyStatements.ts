import { Stack } from 'aws-cdk-lib';
import { PolicyStatements } from '@lib/types';

const usePolicyStatements = (context: Stack): PolicyStatements => {
  return {};
};

export { usePolicyStatements };
