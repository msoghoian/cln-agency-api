import { Stack } from 'aws-cdk-lib';
import { StepFunctionResources } from '@lib/types';

const useStepFunctions = (context: Stack): StepFunctionResources => {
  return {};
};

export { useStepFunctions };
